<div class="container">
  <h1>Add a new Tag to <%= @plant.name %></h1>
  <%= simple_form_for [@plant, @plant_tag] do |f| %>
    <%= f.input :tag, collection: Tag.all.order(:name), label_method: :name %>
    <%= f.submit 'Add Tag', class: 'btn btn-primary' %>
  <% end %>
</div>
